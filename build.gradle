plugins {
    id 'base'
}

// local gradlew
task wrapper(type: Wrapper) {
	gradleVersion = '2.14'
}


task dist(type: Zip) {
    archiveName = "darkduino.zip"
    destinationDir = file(".")
    from "darkduino"
    include "**/*.*"
}
